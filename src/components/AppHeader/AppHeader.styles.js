import styled from 'styled-components';
import { AppWrapper } from '../../styles/global';

export const AppHeader = styled(AppWrapper)`
  background-color: #fff;
  position: sticky;
  top: 0;
  left: 0;
`;